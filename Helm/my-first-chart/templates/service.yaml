apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-fisrt-chart.fullname" . }}
  namespace:  {{ .Values.namespace }}
  labels:
    {{- include "my-fisrt-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-fisrt-chart.selectorLabels" . | nindent 4 }}
